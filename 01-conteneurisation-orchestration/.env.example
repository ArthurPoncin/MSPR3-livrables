# =====================================================================
# Secrets obligatoires : sans ces 2 variables, le compose ne demarre pas
# =====================================================================

# Secret HMAC utilise pour signer les JWT (partage entre auth et api).
# Minimum 64 bytes pour HS512. Generer une valeur via :
#   openssl rand -base64 64
BETTER_AUTH_SECRET=

# Cle API Resend pour l'envoi des emails de verification et de reset.
# La cle est chiffree dans secrets/resend.enc et dechiffree automatiquement
# par bootstrap.sh (passphrase fournie par l'equipe lors de la soutenance).
# Pour un deploiement autonome, obtenir une cle sur https://resend.com/api-keys
# et la renseigner directement ici.
RESEND_API_KEY=


# =====================================================================
# Overrides optionnels : les valeurs par defaut sont deja fixees dans le
# compose. Decommenter uniquement pour modifier.
# =====================================================================

# Mot de passe de la base healthai (metier)
# Laisse vide, bootstrap.sh genere une valeur forte au premier lancement.
# DB_PASSWORD=password

# Config de la base auth (better-auth)
# AUTH_DB_PASSWORD est aussi auto-genere par bootstrap.sh si absent.
# AUTH_DB_USER=root
# AUTH_DB_PASSWORD=password
# AUTH_DB_NAME=auth_db

# URL publique du service auth
# BETTER_AUTH_URL=http://localhost:3000

# Origine CORS autorisee par le service auth
# CORS_ORIGIN=http://localhost:5173

# Planification cron de l'ETL (format cron)
# CRON_SCHEDULE=0 2 * * *

# Nom de la base Mongo pour reco-fitness
# MONGO_DATABASE=reco_fitness

# =====================================================================
# IA generative (ai-nutrition)
# Mistral est utilise en primaire (latence ~2s), Ollama (gemma3:4b) en fallback.
# La cle Mistral est chiffree dans secrets/mistral.enc et dechiffree
# automatiquement par bootstrap.sh (meme passphrase que Resend).
# Pour forcer Ollama uniquement : DEFAULT_LLM=ollama + laisser MISTRAL_API_KEY vide.
# =====================================================================

DEFAULT_LLM=mistral
MISTRAL_API_KEY=

# =====================================================================
# Monitoring (overlay docker-compose.monitoring.yml, optionnel)
# =====================================================================

# Identifiants admin Grafana (http://localhost:3001)
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=admin
